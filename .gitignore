# Personal working notes (not part of the product knowledge base)
docs/Self/

# Dependencies
node_modules/

# Build output — generated by `npm run build` (compiled TS + copied static
# assets), this is what gets deployed to Firebase Hosting. Not committed;
# see docs/decision-log.md (checkpoints 0.5 and 0.6).
public/

# Local, personal Claude Code permission overrides (not shared config)
.claude/settings.local.json

# Firebase CLI's local deploy cache (hosting deploy history/hashes)
.firebase/
