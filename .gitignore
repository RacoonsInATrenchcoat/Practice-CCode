# Personal working notes (not part of the product knowledge base)
docs/Self/

# Dependencies
node_modules/

# Compiled TypeScript output — generated by `npm run build`, not
# source-of-truth. See docs/decision-log.md (checkpoint 0.5 cleanup).
src/*.js
src/**/*.js

# Local, personal Claude Code permission overrides (not shared config)
.claude/settings.local.json
